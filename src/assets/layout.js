const layoutStyles = (theme) => ({
  wrapper: {
    height: "auto",
    minHeight: "100vh",
    position: "relative",
    top: "0",
    backgroundColor: theme.palette.primary.main,
  },
});

export default layoutStyles;
